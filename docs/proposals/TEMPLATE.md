# C-XXX: <수집기 이름>

> 1페이지를 넘기지 않는다. 넘어가면 수집기를 둘로 쪼갤 신호다.

## 무엇을
어떤 데이터를 가져오는가. 한 문단.

## 어디서
- 출처 기관/매체:
- URL:
- 접근 방식: api / rss / crawl / file
- 인증 필요 여부: (API 키가 필요하면 발급 경로도)
- 라이선스·이용약관: public_open / api_tos / crawl_public / restricted

## 왜 필요한가
이 데이터가 최종 산출물(일정표·타깃전략·갭리포트·메시지·게시물배치) 중
무엇에 쓰이는가. 여기에 답을 못 쓰면 만들지 않는다.

## 공통 레코드로의 매핑
- `kind`:
- `geo_level`: / 출처의 지역 코드 → 행안부 행정동코드 변환 방법:
- `observed_at`이 되는 필드:
- `confidence` 근거:
- payload 주요 필드:

## 갱신 주기
- 원 데이터가 갱신되는 주기:
- 수집 주기(cron):
- 증분 수집 가능 여부:

## 제약과 위험
- 예상 데이터량:
- rate limit:
- 출처가 사라지거나 형식이 바뀔 위험:
- 법적 검토가 필요한 부분:
