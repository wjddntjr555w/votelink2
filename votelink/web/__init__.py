"""웹앱(L3) — 저장된 레코드를 읽어서 화면으로 만든다. 규약은 `docs/40-webapp-spec.md`.

**L3는 레코드를 쓰지 않는다.** 이 패키지는 `store.append_records` / `upsert_records` /
`append_rejected` 를 import 하지 않는다. 웹앱이 데이터를 만들기 시작하면 `derived_from`
추적이 끊기고, 화면의 숫자가 어디서 왔는지 말할 수 없게 된다.

문구가 "쓰지 않는다"에서 "**레코드를** 쓰지 않는다"로 벼려졌다 (P-002 §3). 로그인은
본질적으로 쓰기이고, 온보딩은 캠프 설정 파일을 만든다. 지켜야 하는 것은 *서빙이 분석
코퍼스를 변형하지 않는다*이지 *아무것도 쓰지 않는다*가 아니었다. 계정·세션·감사는
`data/control.db` 에, 캠프 설정은 `data/camps/` 에 있다 — 둘 다 레코드 밖이고,
`tests/test_web.py::test_serving_never_writes` 가 수정 없이 계속 통과한다는 사실이
그 경계가 지켜졌다는 증거다.

**이 모듈은 fastapi 를 import 하지 않는다.** 앱 팩토리는 `votelink.web.app` 에 있고,
CLI가 `cmd_serve` 안에서 늦게 불러온다 — 웹 의존성이 없는 환경에서도 `collect`·`analyze`
가 죽지 않아야 하기 때문이다. 여기에는 의존성 없는 상수만 둔다.
"""

DEFAULT_HOST = "127.0.0.1"
"""기본 호스트. **`0.0.0.0` 이 아니다.**

편의가 아니라 컴플라이언스에 인접한 결정이다 — 미검토 산출물이 경고와 함께 뜨는 화면을
LAN에 열어두면 그게 의도치 않은 공표가 된다 (`docs/90-compliance.md §6`).

**근거가 바뀌었다** (P-002 §2). 예전에는 비노출 자체가 인증을 대신했다. 이제는
인증·세션·감사가 그 일을 하고, 공개 표면(`/login`·`/signup`·`/healthz`·`/static`)에는
산출물이 하나도 없다 — 그래서 노출이 공표가 되지 않는다. 대신 조건이 붙는다:
**인증이 꺼져 있으면 `0.0.0.0` 바인딩을 거부한다.** 그 판정은 `cli.py` 의 기동 점검에
있고, 이 상수는 여전히 "따로 말하지 않으면 로컬"이라는 기본값을 지킨다.
"""

DEFAULT_PORT = 8420

LOOPBACK_HOSTS = ("127.0.0.1", "localhost", "::1")
"""이 밖으로 바인딩하는 것이 "노출"이다. `cli.py` 의 기동 점검 둘이 이 목록을 본다 —
인증이 꺼져 있을 때, 그리고 운영자가 아직 배포 기본 비밀번호를 쓸 때."""

__all__ = ["DEFAULT_HOST", "DEFAULT_PORT", "LOOPBACK_HOSTS"]
